{
  "overrides": [
    {
      "files": ["*.js", "*.ts"],
      "extends": "standard-with-typescript",
      "rules": {
        "semi": ["error", "always"],
        "@typescript-eslint/semi": ["error", "always"]
      }
    }
  ]
}
